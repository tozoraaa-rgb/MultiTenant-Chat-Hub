# @mth/admin-app

React/TypeScript admin dashboard application for MultiTenant Chat Hub.

This app remains in the monorepo as a standalone deliverable and can evolve independently from backend and widget packages.
