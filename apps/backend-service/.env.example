# Database connection used by Sequelize bootstrap and app runtime.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=chatbot_db

# Feature 0 seed bootstrap credentials for initial back-office administrator.
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=ChangeMe123!

# Feature 1 authentication settings used for JWT generation and verification.
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN_SECONDS=3600
BCRYPT_SALT_ROUNDS=10
